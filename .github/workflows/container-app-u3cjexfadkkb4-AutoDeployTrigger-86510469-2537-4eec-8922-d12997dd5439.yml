name: Trigger auto deployment for container-app-u3cjexfadkkb4

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/container-app-u3cjexfadkkb4-AutoDeployTrigger-86510469-2537-4eec-8922-d12997dd5439.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to container registry
        uses: docker/login-action@v1
        with:
          registry: containerregistryu3cjexfadkkb4.azurecr.io
          username: ${{ secrets.CONTAINERAPPU3CJEXFADKKB4_REGISTRY_USERNAME }}
          password: ${{ secrets.CONTAINERAPPU3CJEXFADKKB4_REGISTRY_PASSWORD }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: containerregistryu3cjexfadkkb4.azurecr.io/container-app-u3cjexfadkkb4:${{ github.sha }}
          file: ./Dockerfile
          context: ./


  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.CONTAINERAPPU3CJEXFADKKB4_AZURE_CREDENTIALS }}


      - name: Deploy to containerapp
        uses: azure/CLI@v1
        with:
          inlineScript: |
            az config set extension.use_dynamic_install=yes_without_prompt
            az containerapp registry set -n container-app-u3cjexfadkkb4 -g yelpcampb50b --server containerregistryu3cjexfadkkb4.azurecr.io --username  ${{ secrets.CONTAINERAPPU3CJEXFADKKB4_REGISTRY_USERNAME }} --password ${{ secrets.CONTAINERAPPU3CJEXFADKKB4_REGISTRY_PASSWORD }}
            az containerapp update -n container-app-u3cjexfadkkb4 -g yelpcampb50b --image containerregistryu3cjexfadkkb4.azurecr.io/container-app-u3cjexfadkkb4:${{ github.sha }}
